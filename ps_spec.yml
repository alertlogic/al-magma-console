version: 1.0
service_name: al-magma-console

stages:
    -
        name: PR Test
        when:
            - pull_request
            - pull_request:
                trigger_phrase: test it
        image: node:14
        compute_size: small
        # env:
        #    NX_BRANCH: $(ALPS_PR_NUMBER)
        commands:
            - echo ALPS_PR_NUMBER
            - echo ${ALPS_PR_NUMBER}
            - echo $ALPS_PR_NUMBER
            - echo done

